# Handoff — 2603141321

## This Session

### Commit 1 (2603141321) — Phase 1 dashboard — parser + server + UI

Built the complete Phase 1 read-only dashboard.

- `dashboard/projects.py` — parses ROADMAP.md and handoff files for all projects in MpmWorkspace
- `dashboard/server.py` — Flask server on port 5100; `/api/projects`, `/api/refresh`
- `dashboard/templates/index.html` — light monotone UI, multi-column board
- `dashboard/requirements.txt` — flask>=3.0
- Phase 1 ROADMAP items all checked off

### Commit 2 (2603141516) — Dashboard UI overhaul — timeline, docs modal, header redesign

Extensive UI iteration based on live feedback.

- **Streamline**: vertical line centered through cards (passes behind via z-index); JS calculates exact start/end from first/last card midpoints; updates on card expand/collapse
- **Current session card**: reads `git log --after=<last handoff timestamp>` to show unhandoff'd commits; highlighted border
- **Handoff cards**: collapsed = date + all commit summaries (one line each, truncated); expanded = full commit details + Next Tasks
- **Docs modal**: `[docs]` icon button per project; tabs for ROADMAP / README / ARCHITECTURE; server-side markdown rendering via Python `markdown` lib; `/api/doc/<project>/<doctype>` endpoint
- **Column header (sticky)**: project name + docs icon + Phase N info + progress bar (6px) + goal (40px fixed, line-clamp:2) + tasks (40px fixed, flex-shrink:0 per row); click header to toggle goal+tasks; state persists across 30s re-renders
- **Empty handoffs skipped**: old Korean-format handoffs with 0 commits not rendered
- **Width fix**: `max-width + overflow:hidden` on `.col` to prevent variable widths

## Previous Sessions

2603140119 — Project scaffold, CLAUDE.md, ROADMAP revised (Phase 1 dashboard-first, Phase 2 agent, Phase 3 gateway)

## Next Tasks

- [ ] UI polish: fade/gradient on clipped tasks in collapsed state
- [ ] Phase 2 planning with user: daemon + Claude Code CLI session management
