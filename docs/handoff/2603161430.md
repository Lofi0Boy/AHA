# Handoff — 2603161430

## This Session

### Commit 1 (2603161218) — 키보드 UX 설계 + Shift+Space 세션 전환 단축키
- `docs/keyboard_ux.md` 신규 작성: 레이어 모델(Board→Terminal→Modal→Dialog→TextInput), 존 네비게이션(컬럼 간 좌우, 아이템 간 상하), 글로벌 단축키 정의
- `docs/ARCHITECTURE.md`에 키보드 UX 섹션 추가 (keyboard_ux.md 참조)
- Shift+Space 세션 순환 스위칭 구현 (보드 레이어에서만 작동하는 초기 버전)

### Commit 2 (2603161430) — xterm.js 직접 연결 + 터미널 캐싱
- **iframe 제거 → xterm.js 직접 연결**: ttyd iframe을 없애고 xterm.js가 ttyd WebSocket에 직접 접속. `attachCustomKeyEventHandler`로 터미널 내부에서도 키보드 단축키 가로채기 가능
- **배경**: iframe은 cross-origin이라 부모 페이지에서 키 이벤트 가로채기 불가. same-origin 프록시(aiohttp)를 시도했으나 구조가 과도하게 복잡해져서 xterm.js 직접 연결로 전환
- **프로젝트별 터미널 캐싱**: 세션 전환 시 destroy/recreate 대신 `display:none/block`으로 즉시 스위칭. WebSocket 연결 유지
- **출력 버퍼링**: requestAnimationFrame 단위 flush + flow control (PAUSE/RESUME)
- **ttyd token 프록시**: `/api/ttyd-token/` 엔드포인트 추가 (CORS 우회)
- **깜빡임 이슈**: Claude Code 자체 버그 (GitHub 700+ upvote). 우리 코드 문제 아님

### Commit 3 (2603161500) — tmux mouse 복원 + 터미널 우클릭 정리
- tmux `mouse on` 유지 — Shift+드래그로 텍스트 선택, Shift+Insert로 붙여넣기
- 터미널 영역에서 브라우저 우클릭 컨텍스트 메뉴 차단 (tmux 우클릭만 작동)

### Commit 4 (2603161540) — 터미널 UX 개선 모음
- **활성 프로젝트 딤 효과**: 터미널 열리면 board-wrapper에 10% 검정 오버레이, 활성 컬럼만 z-index로 상위 표시. 노트(z-500)도 딤 아래로
- **Esc로 터미널 닫기**: xterm attachCustomKeyEventHandler에서 Escape 가로채기
- **좀비 ttyd 자동 정리**: 서버 재시작 후 포트 충돌 시 `fuser`로 기존 프로세스 kill → running인데 터미널 안 열리던 버그 수정
- **커밋 비교 초 단위**: `--after` 포맷을 `%H:%M:%S`로 변경 → 푸시+핸드오프 동시 시 중복 표시 방지

## Previous Sessions

2603161218 — 터미널 UX 버그 수정 (스크롤/우측패널/드래그/위치저장/오버레이), xterm.js PTY bridge + CLI 시작 다이얼로그 + 터미널 카드, ttyd 전환 + inotify 파일 감지 + 터미널 패널 리사이즈/우측 토글

2603151818 — 포스트잇 노트, 문서 한글화 키워드 복원, Phase 2 ROADMAP 재정의, tmux 세션 관리 + Gateway API, WebSocket multiplexer + 터미널 뷰

2603141524 — Phase 1 대시보드 구축 + UI 개선

2603140119 — 프로젝트 스캐폴드, ROADMAP 수정

## Next Tasks

- [ ] 키보드 조작 UX 완성 — 존 네비게이션 (좌우 컬럼 이동, 상하 아이템 이동, Enter/Space 활성화)
- [ ] 터미널 활성 프로젝트 시각 효과
- [ ] Agent 상태 세분화 (응답 중 / 응답 완료 / 유휴 / 꺼짐)
- [ ] 응답 완료 인터랙션
- [ ] 비서 Agent
- [ ] Telegram 브릿지
