# Handoff — 2603191743

## This Session

### Commit 1 (2603191743) — 태스크 시스템 v2 구현 + Current Task 카드 실시간 상태 표시

**설계 논의 → 구현**까지 한 세션에서 수행.

**설계 결정 (docs/design/task-system-v2.md에 정리됨):**
- 배포: standalone 패키지 (대시보드 메인) + `mpm init`으로 `.claude/` 주입
- 태스크: 통합 스키마 (future/current/past 동일), 어트리뷰트가 점진적으로 채워짐
- 강제 방식: Rules(인지) + Skills(실행) + Hooks(안전망+알림) 3단 조합
- 결과 판단: 항상 유저 확인, 자연어로 응답하면 Claude가 status 매핑

**구현:**
- `.mpm/` 새 구조: `docs/PROJECT.md`, `data/{future.json, current/, past/}`
- `.claude/rules/mpm-workflow.md` — 태스크 시스템 규칙
- `.claude/skills/mpm-next/SKILL.md` — `/mpm-next` 커맨드
- `.claude/skills/mpm-init-project/SKILL.md` — 프로젝트 스캔 + 인터뷰
- `.claude/settings.json` — SessionStart/UserPromptSubmit/Stop hooks
- `.mpm/scripts/task.py` — 태스크 전이 CLI (pop/complete/add/status/update)
- `.mpm/scripts/hook-notify.sh` — hook → 대시보드 HTTP 알림 스크립트
- `dashboard/projects.py` — v2 데이터 로더 (future/current/past + legacy compat)
- `dashboard/server.py` — v2 API + hook 수신 엔드포인트
- `dashboard/templates/index.html` — current task 카드, future 큐, past 히스토리 렌더링

**실시간 상태 파이프라인:**
- UserPromptSubmit hook → curl "working" → server → WebSocket → 파란 보더 펄스
- Stop hook → curl "waiting" → server → WebSocket → 주황 보더 펄스
- 실제 동작 확인 완료

**버그 수정:**
- `updateStatusDots`가 `data-term-project` 속성 기준으로 모든 카드 className을 덮어쓰던 문제 → `current-task-card`는 스킵하도록 수정
- prompt 타입 hook JSON validation 에러 → command 타입만 사용하도록 변경 (prompt hook은 추후 재도입)

## Previous Sessions

2603181916 — 터미널 패널 초기 로드 시 플래시 제거

2603181226 — BroadcastChannel 기반 팝아웃↔메인 통신 전면 교체, 포커스 기반 활성 프로젝트 추적, 서버 재시작 세션 보존, Flask 캐싱 비활성화

2603171155 — systemd 서비스 등록, 팝아웃 터미널, Since Last Handoff 기준 변경

2603161538 — 키보드 UX 설계, xterm.js 직접 연결 + 터미널 캐싱

2603161218 — 터미널 UX 버그 수정, ttyd 전환 + inotify 파일 감지

2603151818 — 포스트잇 노트, Phase 2 ROADMAP, tmux 세션 관리 + Gateway API

2603141524 — Phase 1 대시보드 구축

2603140119 — 프로젝트 스캐폴드

## Next Tasks

- [ ] Current Task 카드 + Past Task 카드 UI 다듬기
- [ ] 대기 태스크 큐 표시
- [ ] 에이전트 상태 인디케이터 (WebSocket)
- [ ] 문서 관리 탭
- [ ] prompt 타입 Stop hook 재도입 (셀프리뷰)
